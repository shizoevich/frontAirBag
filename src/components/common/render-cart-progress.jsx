import React from "react";

// Free-shipping логика отключена по требованию: компонент не рендерит никакого текста/прогресса.
// Подсказка теперь выводится отдельно в cart-mini через t('discountHint').
const RenderCartProgress = () => null;

export default RenderCartProgress;
